<!-- Dropdown Structure -->
<ul id="dropdown1" class="dropdown-content">
    <li><a id="history" href="/history">History</a></li>
    <li><a id="feed" href="#!">Feed</a></li>
    <li><a id="signout" href="/">Signout</a></li>
</ul>
<nav>

    <div class="nav-wrapper">
        <div class="row">

            <div class="col s4">
                <a href="/home" class="brand-logo"> <i class="material-icons">home</i></a>
            </div>

            <div class="col s4">
                <h2 id="title">Memorybank</h2>
            </div>

            <div class="col s4">
                <ul class="right hide-on-med-and-down">

                    <!-- Dropdown Trigger -->
                    <li>
                        <a class="dropdown-trigger" href="/home" data-target="dropdown1">
                            <i class="material-icons right">dehaze</i></a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
<div class="container" id="welcomeContainer">

    <h1 id="welcomeH1">Welcome {{users.first_name}}</h1>

    <div id="hororscopeForDay">
        <p>Today's Date and Horrorscope. Placeholder text below</p>
        <p>{{histories.description}}</p>
    </div>

    <div id="otherLuckyInfo">
        <p>Lucky color, lucky number, lucky time, compatibility with other signs. Placeholder text below</p>
        <p>Lorem ipsum, dolor sit amet consectetur adipisicing elit. Tenetur pariatur perspiciatis deleniti debitis
            distinctio maxime minus iure, impedit placeat voluptate quia eius nulla unde vel ex incidunt temporibus
            repellendus excepturi.</p>
    </div>
    <br>
    <br>
    <br>

    <div id="uploadArea">
        {{!-- <a class="waves-effect waves-light btn" id="memory">Upload Memory Picture</a> --}}
        <button id="upload_widget" class="cloudinary-button">Upload files</button>

        <script src="https://widget.cloudinary.com/v2.0/global/all.js" type="text/javascript"></script>

        <script type="text/javascript">
            var myWidget = cloudinary.createUploadWidget({
                cloudName: 'dl5nddb9b',
                uploadPreset: 'awfcnrld'
            }, (error, result) => {
                if (!error && result && result.event === "success") {
                    console.log('Done! Here is the image info: ', result.info.secure_url);

                    //Cloudinary sends a post request to the /image route to the backend containg a string of data containg the image url to cloudinary
                    $.post("/image", {
                        memory_image: result.info.secure_url
                    })


                        .then((data) => {
                            console.log(data, "saved image")
                            location.reload()
                        })
                        .fail((err) => {
                            console.log(err)
                        })



                }
            }
            )

            document.getElementById("upload_widget").addEventListener("click", function () {
                myWidget.open();
            }, false);




        </script>

    </div>
    <p>{{histories.memory_image}}</p>
    <br>

    <div class="row">
        <form class="col s12">
            <div class="row">
                <div class="input-field col s12">
                    <textarea id="textarea1" class="materialize-textarea" rows="3" cols="80"></textarea>
                    <label for="textarea1">Write your Journal Entry Here!</label>
                </div>
            </div>
        </form>
    </div>

    <div id="pubPriv">
        <label> <input type="checkbox" class="filled-in" name="public_status" id="public_status"
                value="false"><span>Check the box to
                make this post public</span></label>

        {{!-- on the review page, you should use a key for the value so that it's set to whatever they set it to when they submitted it --}}

        {{!-- Should also add approximately this code in your JS (may have to tweek it a bit)
        $("#public_status").on('change', function() {
            if ($(this).is(':checked')) {
                $(this).attr('value', 'true');
            } else {
                $(this).attr('value', 'false');
            } --}}
    </div>

    <div id="submitButton">
        <a class="waves-effect waves-light btn" id="submit">Submit</a>
    </div>
</div>